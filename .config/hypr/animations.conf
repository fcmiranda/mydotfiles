# # animation

# Original (slower, smooth)
# animations {
#     enabled = yes
#     bezier = ease, 0.25, 0.1, 0.25, 1.0
#     bezier = overshot, 0.13, 0.99, 0.29, 1.05
#     animation = windows, 1, 5, overshot, gnomed
#     animation = windowsOut, 1, 5, ease, slide bottom
#     animation = windowsMove, 1, 5, overshot, slide
#     animation = layers, 1, 5, ease, gnomed
#     animation = fade, 1, 3, ease
#     animation = border, 1, 2, ease
#     animation = workspaces, 1, 5, overshot, slide
# }

# Medium speed (current)
# animations {
#     enabled = yes
#     bezier = ease, 0.25, 0.1, 0.25, 1.0
#     bezier = overshot, 0.13, 0.99, 0.29, 1.05
#     animation = windows, 1, 3, overshot, gnomed
#     animation = windowsOut, 1, 3, ease, slide bottom
#     animation = windowsMove, 1, 3, overshot, slide
#     animation = layers, 1, 3, ease, gnomed
#     animation = fade, 1, 2, ease
#     animation = border, 1, 1, ease
#     animation = workspaces, 1, 3, overshot, slide
# }

# Fast & snappy
animations {
    enabled = yes
    bezier = ease, 0.25, 0.1, 0.25, 1.0
    bezier = overshot, 0.13, 0.99, 0.29, 1.05
    animation = windows, 1, 2, overshot, gnomed
    animation = windowsOut, 1, 2, ease, slide bottom
    animation = windowsMove, 1, 2, overshot, slide
    animation = layers, 1, 2, ease, gnomed
    animation = fade, 1, 1, ease
    animation = border, 1, 1, ease
    animation = workspaces, 1, 2, overshot, slide
}

# Maximum speed
# animations {
#     enabled = yes
#     bezier = ease, 0.25, 0.1, 0.25, 1.0
#     bezier = overshot, 0.13, 0.99, 0.29, 1.05
#     animation = windows, 1, 1, overshot, gnomed
#     animation = windowsOut, 1, 1, ease, slide bottom
#     animation = windowsMove, 1, 1, overshot, slide
#     animation = layers, 1, 1, ease, gnomed
#     animation = fade, 1, 1, ease
#     animation = border, 1, 1, ease
#     animation = workspaces, 1, 1, overshot, slide
# }

# Smooth & elegant (macOS-like)
# animations {
#     enabled = yes
#     bezier = smooth, 0.4, 0.0, 0.2, 1.0
#     bezier = easeInOut, 0.42, 0.0, 0.58, 1.0
#     animation = windows, 1, 4, smooth, popin 80%
#     animation = windowsOut, 1, 4, smooth, popin 80%
#     animation = windowsMove, 1, 4, smooth
#     animation = layers, 1, 4, smooth, slide
#     animation = fade, 1, 4, smooth
#     animation = border, 1, 5, easeInOut
#     animation = workspaces, 1, 4, smooth, slidefade 20%
# }

# Bouncy & playful
# animations {
#     enabled = yes
#     bezier = bounce, 0.68, -0.55, 0.265, 1.55
#     bezier = easeOut, 0.0, 0.0, 0.2, 1.0
#     animation = windows, 1, 5, bounce, popin
#     animation = windowsOut, 1, 4, easeOut, popin 80%
#     animation = windowsMove, 1, 4, bounce
#     animation = layers, 1, 4, bounce, slide
#     animation = fade, 1, 3, easeOut
#     animation = border, 1, 3, easeOut
#     animation = workspaces, 1, 5, bounce, slide
# }

# Minimal (almost instant)
# animations {
#     enabled = yes
#     bezier = linear, 0.0, 0.0, 1.0, 1.0
#     animation = windows, 1, 1, linear, popin
#     animation = windowsOut, 1, 1, linear, popin
#     animation = windowsMove, 1, 1, linear
#     animation = layers, 1, 1, linear
#     animation = fade, 1, 1, linear
#     animation = border, 0
#     animation = workspaces, 1, 1, linear, fade
# }

# Windows 11 style
# animations {
#     enabled = yes
#     bezier = fluent, 0.0, 0.0, 0.0, 1.0
#     bezier = fluentOut, 1.0, 0.0, 1.0, 1.0
#     animation = windows, 1, 3, fluent, popin 95%
#     animation = windowsOut, 1, 2, fluentOut, popin 95%
#     animation = windowsMove, 1, 3, fluent
#     animation = layers, 1, 3, fluent, slide
#     animation = fade, 1, 2, fluent
#     animation = border, 1, 4, fluent
#     animation = workspaces, 1, 3, fluent, slidefade 10%
# } 